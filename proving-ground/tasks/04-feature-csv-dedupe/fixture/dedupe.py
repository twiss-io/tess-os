def dedupe_rows(rows, key_fields):
    """Dedupe `rows` (a list of dicts) by the composite key formed from
    `key_fields`, keeping the first occurrence of each distinct key in
    original relative order. See brief.md in this task for the full,
    authoritative spec — implement exactly to it."""
    raise NotImplementedError
