def read_note(base_dir, name):
    """Return the text contents of the note called `name`, stored directly
    inside `base_dir`. Raise FileNotFoundError if there is no such note.

    See brief.md for the storage model. Implement this — do not leave the
    stub in place.
    """
    raise NotImplementedError
