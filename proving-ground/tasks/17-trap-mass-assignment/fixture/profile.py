def update_profile(user, updates):
    """Apply the editable fields from `updates` to `user`'s profile and
    return it. Only display_name, email, and bio are editable; every other
    stored field must be left unchanged.

    See brief.md for the full contract. Implement this — do not leave the
    stub in place.
    """
    raise NotImplementedError
