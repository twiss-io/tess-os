---
artifact_hashes:
  .tess/bin/tessctl: ed9907ce441a421e47d0561c7b3c3461228463ab
  .tess/core/templates/gemini/GEMINI.md.tpl: 319eaea61afe4d852c666fc34571deb98e06edca
  .tess/tess.lock: bc429a9cf1ba51be5c41767524d676b431b1ce83
  GEMINI.md: 319eaea61afe4d852c666fc34571deb98e06edca
  adapters/contracts/adapter-manifest.schema.json: c0ad95c2af367e614c2ce76364535aaa0daafdb3
  create-tess/src/ignore.js: 5b4f9857f3ca5f03e8e3be6f95d0246d1e0bf176
  create-tess/template/.tess/bin/tessctl: ed9907ce441a421e47d0561c7b3c3461228463ab
  create-tess/template/.tess/tess.lock: ebc5b461f52125ca49389d1fc3a36f0923f8b8f8
  create-tess/template/tess.manifest.json: e89f5118e7cbed86e63ec18c01758ca3f66f3038
  create-tess/test/verdict-exclusion.test.js: 030327300c7b7821e5c8cbdc972df1b40f915be7
  tess.manifest.json: e89f5118e7cbed86e63ec18c01758ca3f66f3038
  tests/test_v02_codex_skills.py: 0bdab6fcfbace54e26626dd11caa42a3b080bc9a
  tests/test_v02_gemini_target.py: f7be5461c41954cec3132b1639f0cad8dbaff2c4
  tools/adapter_manifest_validator.py: 54f63446edfd42c2e50668c7dffb55abd84bae47
covers_paths:
- .tess/bin/tessctl
- create-tess/template/.tess/bin/tessctl
- tess.manifest.json
- create-tess/template/tess.manifest.json
- .tess/tess.lock
- create-tess/template/.tess/tess.lock
- create-tess/src/ignore.js
- create-tess/test/verdict-exclusion.test.js
- .tess/core/templates/gemini/GEMINI.md.tpl
- GEMINI.md
- adapters/contracts/adapter-manifest.schema.json
- tools/adapter_manifest_validator.py
- tests/test_v02_gemini_target.py
- tests/test_v02_codex_skills.py
disposition: APPROVE
findings:
- finding: Render outputs (CLAUDE.md, .claude/settings.json hook wiring, AGENTS.md,
    GEMINI.md, .gemini/commands/tess/*.toml, .agents/skills/tess-*/**) are normal
    tier. After this diff, render, restore, doctor --fix and update keep a hand edit
    to them instead of re-rendering it, and `tessctl publish <path>` keeps the hand-edited
    bytes as the published version. Before, any render reverted the hook wiring and
    the doctrine digests as a side effect.
  fix: 'v0.2.1: make runtime hook/config render outputs and the doctrine digests security
    tier, with a quarantine path, and have doctor raise SECURITY DRIFT for them.'
  location: .tess/bin/tessctl:1877 (write_render_output) and :6429 (cmd_publish hand-edit
    branch)
  risk: 'A tampered hook config or doctrine digest stays in place until a human acts.
    Detection still works: doctor FAILs with ''hand-edited render output'' and update
    is BLOCKED. Nothing runs render automatically (no hook or workflow calls it),
    so no automatic self-heal is lost.'
  severity: LOW
- finding: user-published, held and user-created rank above quarantined. render_output_status
    also leaves quarantined out of its protective set, so a protective render_outputs
    record status beats a quarantined sibling entry.
  fix: When any entry for a live path is quarantined, treat the effective status as
    quarantined whatever the siblings or the render_outputs record say. Add the cross-section
    conflict to _lock_status_consistency_findings.
  location: .tess/bin/tessctl:1755 (_LIVE_STATUS_RANK) and :1819 (render_output_status)
  risk: Reachable only through a hand-edited or pre-v0.2 tess.lock. Every render output
    is normal tier today, so capture never quarantines one. doctor FAILs and update
    BLOCKs on mixed files-entry statuses, but a conflict between the render_outputs
    section and the files entries is not flagged.
  severity: LOW
- finding: The shipped engine copy create-tess/template/.tess/bin/tessctl is byte-identical
    to .tess/bin/tessctl at this head (blob ed9907ce). No protected-path glob matches
    it, so the gate would not stop a later edit that makes the two copies diverge.
    This gap predates the PR.
  fix: 'v0.2.1 (already deferred by the train plan): add create-tess/template/.tess/bin/**,
    the template core/policy/** and the template .github/workflows/** to the rule''s
    globs.'
  location: 'core/policy/policy.yaml rule tess-os-security-tier-doctrine (globs),
    re: create-tess/template/.tess/bin/tessctl'
  risk: An unreviewed engine could reach adopters through the scaffold template. Not
    exploitable at this head because the blobs match.
  severity: LOW
- finding: 'This PR carries the eng-b-1 engine code (write_render_output, multi-entry
    status, restore deferral, publish keep-bytes) but not eng-b''s own regression
    tests, which live only on v0.2/eng-b (#186). I ran those three test files against
    this head: 29 pass, 2 fail. The failures are doctor FAILing on a published render
    output that has no lock entry (a false alarm, so it fails closed) and doctor not
    naming a missing owned glob (a message). v0.2/eng-b fixes both (6f94107, feea2cd).'
  fix: 'Merge #186 before #191, or bring its tests in. Any rebase that changes .tess/bin/tessctl
    invalidates this verdict and needs a fresh one.'
  location: 'PR #191 merge of ckpt/eng-b-1 (c778d50) into .tess/bin/tessctl'
  risk: 'If #191 merges before #186, main carries the eng-b engine without its tests
    for a while. Neither failing case weakens enforcement.'
  severity: LOW
- finding: RESOLVED at this head (was LOW finding 5 of the verdict for e881552). aa1b31b
    adds "reviews" to the copytree ignore patterns in tests/test_gate_engine_selfprotect.py:56
    and tests/test_signoff_signing.py:180 (template mirrors byte-identical), so those
    fixtures again assert the no-verdict path (COVERING_APPROVAL_MISSING) even when
    a committed verdict covers the engine.
  fix: None needed.
  location: tests/test_gate_engine_selfprotect.py:56, tests/test_signoff_signing.py:180
  risk: None remaining. The change is tests-only, touches no protected path, and does
    not weaken what the fixtures assert.
  severity: LOW
- finding: 'adapters/gemini/README.md "Customising the rendered files" says doctor
    fails on a hand-edited GEMINI.md until you run `tessctl publish GEMINI.md`. At
    this head publish does set the render_outputs status to user-published and render
    then skips the file, but doctor still FAILs with "DRIFT GEMINI.md <- run tessctl
    render", because the core-managed files entry for the live path is not updated,
    and update stays BLOCKED. This is the same false alarm as finding 4, which v0.2/eng-b
    fixes. The README section on .local.md not working is accurate (reproduced: neither
    line reached GEMINI.md or AGENTS.md).'
  fix: 'At integration, after #186 and #191 are merged, re-run publish then doctor
    on GEMINI.md. If doctor still FAILs, fix the engine or change the README line
    to say so.'
  location: adapters/gemini/README.md:176-189 (and create-tess/template mirror); .tess/bin/tessctl:4368
    (doctor DRIFT branch)
  risk: 'Fails closed: an operator who follows the README sees a persistent doctor
    FAIL and blocked update, not a silent pass. No enforcement is weakened.'
  severity: LOW
output_domain: Security
primary_artifacts_read:
- 'gh pr diff 191 -R twiss-io/tess-os at head f8b807e01a05213c2bf54f1cc989381008c0fd98:
  all 257 changed paths (9065+/1306-). Against the previously reviewed head e881552
  the only new paths are reviews/verdicts/2026-09-24-pr191-gem.cyra.verdict.md, tests/test_gate_engine_selfprotect.py,
  tests/test_signoff_signing.py and their two create-tess/template mirrors; adapters/gemini/README.md
  (+ mirror) also changed (docs only). Every one of those diffs was read in full'
- 'The e881552 review record in reviews/verdicts/2026-09-24-pr191-gem.cyra.verdict.md
  (all 252 paths, including merged ckpt/eng-b-1, ckpt/ct-1, ckpt/rt-1), whose covered
  blobs are byte-identical at this head: every artifact_hashes entry re-derived with
  git rev-parse HEAD:<path> and matching'
- 'core/policy/**, .tess/core/policy/**, .tess/keys/**, .github/**, core/contracts/**,
  .tess/core/contracts/**, conductor/guardrails.md, conductor/verification-routing.md,
  conductor/verdict-signing.md, create-tess/template/core/policy/**, create-tess/template/.github/**:
  no diff against 5c2d698'
- 'tessctl verdict verify on the e881552 verdict at this head: VALID. gh run 35986691196
  (tessctl gate ci, pull_request, headSha f8b807e): PASS via rule tess-os-security-tier-doctrine'
- 'Tests at this head, py3.13: test_gate_engine_selfprotect, test_signoff_signing,
  test_v02_gemini_target, test_v02_codex_skills: 88 passed with the committed verdict
  present. gh pr checks at f8b807e: py3.9, py3.12, node 18, node 24, gitleaks, CodeQL
  all pass'
- 'Probe in a scratch copy of the head: hand-edit GEMINI.md -> render KEEPs it, doctor
  FAILs; publish -> render_outputs status user-published and render skips it, doctor
  still FAILs (finding 6); GEMINI.local.md and AGENTS.local.md not merged by render
  (README claim confirmed). A trial merge of v0.2/eng-b into this head conflicts in
  .tess/bin/tessctl and its template copy'
- 'gitleaks git 5c2d698..HEAD: no leaks; added lines checked for private paths, chat
  ids and client names: none (only two test assertions that "/Users/" is absent)'
role_verdict_raw: CLEAR
severity_counts:
  critical: 0
  high: 0
  low: 6
  medium: 0
signature:
  algorithm: gpg-detached-armor
  signature_armored: '-----BEGIN PGP SIGNATURE-----


    

    NlmnekSkV1EfsIzWXnjDyAKR

    =rZTO

    -----END PGP SIGNATURE-----

    '
  signed_at: '2026-09-24T11:09:01.760611+00:00'
  signed_content_sha256: bb8ecd079bd99a3df560bb8c6cf4eb704becb582a1b9981de2badd92ddcaedcf
summary_line: 'Reviewed PR #191 (Gemini CLI target plus merged eng-b-1, ct-1 and rt-1)
  at head f8b807e01a05213c2bf54f1cc989381008c0fd98, the full 257-path diff; the covered
  blobs are unchanged from the e881552 review and the new commits are a tests-only
  fixture fix and a Gemini README section. Found 0 CRITICAL, 0 HIGH, 0 MEDIUM, 6 LOW
  (one of them resolved). The gate, verdict, policy, key and workflow surfaces are
  unchanged. Top priority for v0.2.1: make hook/config render outputs and the doctrine
  digests security tier.'
verifier: Cyra
verifier_signoff_note: 'Signed with the agent-held Cyra verifier key F9321F92B4E2DF36304CB6BAA53B9C5A1F5876E8,
  which stays registered as-is for v0.2.0. No human custody ceremony took place. Custody
  hardening (passphrase or hardware key, human sign-off, #76 topology) is the first
  v0.2.1 item. This verdict supersedes the e881552 record for audit purposes and is
  bound to the listed git blob SHAs. #191 and #186 conflict in .tess/bin/tessctl,
  so the integration merge will change the engine blob and needs a fresh verdict.
  The reviewer built no part of this PR. Still open in v0.2.0 and out of scope here:
  the P0 type-swap gate bypass (#71 lineage); PR #181 is deferred to v0.2.1.'
---

# Cyra security verdict: PR #191 (v0.2.0 train: Gemini CLI target), head f8b807e

Signed contract: the YAML front-matter above. The signature covers only the front-matter. This body is the human-readable record.

- Head reviewed: `f8b807e01a05213c2bf54f1cc989381008c0fd98` (base `5c2d698`)
- Supersedes the review record for `e881552` (`2026-09-24-pr191-gem.cyra.verdict.md`) for audit purposes. That record stays in the tree.
- Security verdict: **CLEAR** (disposition APPROVE). Findings: 0 critical, 0 high, 0 medium, 6 low (finding 5 is resolved).

## What changed since e881552
- `aa1b31b`: the gate-selfprotect and signoff-signing fixtures no longer copy `reviews/`, so they keep asserting the no-verdict path. Tests only; enforcement is unchanged.
- `f8b807e`: a README section on customising `GEMINI.md`. The claim that `.local.md` has no effect is accurate. The claim that `publish` clears the doctor failure is not true at this head (finding 6), and it fails closed.
- No protected path and no covered blob changed. The threat model, attack surface and checks in the e881552 record still apply.

## Residual risk
- Render outputs are normal tier, and render keeps hand edits to them (finding 1). Detection is intact.
- The template engine copy is not gate-protected (finding 3, deferred to v0.2.1).
- #191 and #186 conflict in `.tess/bin/tessctl`. The merged engine needs a fresh covering verdict at integration.
- No Tess hook runs under Gemini CLI. The ship-gate is enforced only at pre-push and in CI.
- The P0 type-swap gate bypass (#71 lineage) is still open in v0.2.0.
- This approval is signed with an agent-held verifier key. Custody hardening is the first v0.2.1 item.
