---
artifact_hashes:
  .github/workflows/publish-npm.yml: 47e487df2df9f7529a6556492f5a54131afc13c2
  .github/workflows/release.yml: 0a5d5a34a9f02159f28e80477d257cb8774f922a
  .tess/bin/tessctl: 9519dff158c26ba2c608c562b79007d509a0b715
  conductor/channel-guardrails.md: 5f1b30429dd4e3dc71f9d3cc70707c0de5107c97
  conductor/dispatch-brief.md: c0917d60972087c281ef5231655f6eb44d161e78
  conductor/guardrails.md: 8fdd6867c93e9ed1debd8091a5a2e3511cdaef99
  conductor/verification-routing.md: c4ff65752fc366e8a8e2df6d76060201bfff096c
  core/policy/policy.yaml: 5c5d162a8f39235550e98d8f2b4b81d43ac911b8
  create-tess/template/.tess/bin/tessctl: 9519dff158c26ba2c608c562b79007d509a0b715
confidence: HIGH
covers_paths:
- .github/workflows/publish-npm.yml
- .github/workflows/release.yml
- .tess/bin/tessctl
- conductor/channel-guardrails.md
- conductor/dispatch-brief.md
- conductor/guardrails.md
- conductor/verification-routing.md
- core/policy/policy.yaml
- create-tess/template/.tess/bin/tessctl
disposition: APPROVE
findings:
- finding: 'Integration fix e126841 routes .codex/agents/<role>.toml through write_render_output.
    That fixes the update crash on pre-0.2.0 manifests, but the per-role files lost
    the self-healing re-pin they had at the approved roster head (aed6bf8). Probe:
    set leah.toml (a read-only role) to sandbox_mode = "danger-full-access", then
    run render, restore and doctor --fix. At aed6bf8 render restores read-only. At
    f4ab914 the edit survives all three, and render prints "rendered .codex/agents/**
    (9 roles)". doctor and verify FAIL (DRIFT, hand-edited render output), so the
    edit is caught. The .claude/agents/<role>.md files behave the same way: kept by
    the drift gate, flagged by doctor.'
  fix: 'v0.2.1: treat Tess-rendered .codex/agents/<installed role>.toml as enforcement
    render outputs (sandbox_mode is sandbox policy under the ENFORCEMENT_RENDER_OUTPUTS
    definition), or pass force=True from _sync_codex_role_agents so a hand edit is
    re-pinned with a snapshot. Add a test that an escalated sandbox_mode does not
    survive render.'
  location: .tess/bin/tessctl:4377 (_sync_codex_role_agents write_render_output call),
    :1886 (ENFORCEMENT_RENDER_OUTPUTS)
  risk: An agent with workspace write access can raise a read-only role's Codex sandbox
    up to the parent session's policy, and the change persists across render and update
    until an operator acts on doctor. Codex docs say subagents take the parent's live
    sandbox overrides, and they document no way for a custom agent to go above the
    parent session. The same agent could already add a new foreign agent file, and
    nothing flags that.
  severity: MEDIUM
- finding: 'The known upgrade issue (untracked .tess/core/roster-paths.json and templates/claude-md/hard-floor.md)
    is not a security issue in itself. The files keep the operator''s pre-upgrade
    bytes, and doctor and verify FAIL loudly. But: (a) it hits every 0.1.x scaffold,
    not an edge case. The pristine create-tess@0.1.4 scaffold shipped both files untracked
    with different bytes. (b) The CHANGELOG remedy does not work. `tessctl lock --regen
    --only <path> --yes` exits with "path not found in tess.lock". (c) The engine''s
    own A2 remedy ("delete it and re-run `tessctl update`") adopts both files, but
    Step 6 fast-forwards CLAUDE.md before A2 adopts hard-floor.md. The live CLAUDE.md
    then gets "<!-- MISSING: .tess/core/templates/claude-md/hard-floor.md -->" in
    place of its whole hard-floor block. Step 7 keeps that as a hand edit and update
    exits 0. doctor then reports SECURITY DRIFT on CLAUDE.md. `tessctl reset CLAUDE.md`
    restores it, and doctor and verify are then OK (probed end to end on the OTA kit
    scaffold).'
  fix: 'Before tagging (docs only, outside protected paths): correct CHANGELOG.md:45
    to say every 0.1.x upgrade is affected, and give the working remedy: delete the
    two files, run `tessctl update --ref v0.2.0` again, then `tessctl reset CLAUDE.md`,
    then confirm `tessctl doctor`. v0.2.1: run A2 adoption before the Step 6 fast-forward
    (or re-render composed outputs after A2 without treating Tess''s own Step 6 write
    as a hand edit), make update exit non-zero when it keeps a security-tier render
    output, and adopt upstream bytes for a pre-0.2 untracked core file that is byte-identical
    to the file 0.1.x shipped.'
  location: CHANGELOG.md:45; .tess/bin/tessctl:7602 (A2 kept-file WARN), :7476/:7810
    (Step 5-6 fast-forward) before :7533 (A2) and :7709 (Step 7)
  risk: An operator who follows the engine's printed remedy gets a conductor CLAUDE.md
    with no hard-floor, verification-routing or clarification-floor summary, and update
    reports success. The full Rule 18 stays in conductor/guardrails.md. doctor and
    verify FAIL until reset. The problem is recoverable and visible, but only if someone
    runs doctor, and doctor is not wired to run at session start.
  severity: MEDIUM
- finding: 'Stale or misleading output around the Codex role mirror. render prints
    "rendered .codex/agents/** (N roles)" even when a role file was skipped as hand-edited,
    or not written at all as not-owned (on the upgraded 0.1.4 scaffold: NOTE not written,
    then "rendered .codex/agents/** (8 roles)"). The CodexRenderTarget docstring still
    says it does not render .codex/agents/, and the render() comment says the files
    are outside render_outputs bookkeeping, which is no longer true after e126841.'
  fix: v0.2.1 - count only 'written'/'unchanged' results and report skips; refresh
    the docstring and comment.
  location: .tess/bin/tessctl:4631, :4581
  risk: Cosmetic. It can hide M1 from someone who reads render output but not doctor.
  severity: LOW
- finding: The e126841 engine fix (update crashed on every pre-0.2.0 manifest) has
    no pytest regression test. It was found only by the OTA rehearsal, and the full
    suite passed both before and after the fix.
  fix: 'v0.2.1: a test that renders the codex target with a 0.1.4 manifest (tests/fixtures/manifest_0_1_4.json)
    and asserts skipped:not-owned, exit 0 and the NOTE line.'
  location: tests/ (none added in e126841)
  risk: A future change could bring the upgrade crash back with the suite still green.
  severity: LOW
- finding: The guardrails.md v0.2.0 CHANGELOG entry says Rule 1 describes "the two
    dispatch guards" as warn-mode, but after notg the Rule 1 body describes one guard,
    dispatch-guard.sh (the chat-channel guard was removed).
  fix: 'v0.2.1: say "the dispatch guard".'
  location: conductor/guardrails.md:322
  risk: Doctrine accuracy only. No rule is weakened.
  severity: LOW
output_domain: Security
primary_artifacts_read:
- 'PR #199 (twiss-io/tess-os), branch v0.2/integration, head f4ab91447497cd4867413680d2a3e83cacb89447,
  base origin/main 5c2d698, read in a separate worktree (tessos-v02/cyra-release).
  All 14 merge parents match the listed PR heads (dep 6986278, ct dd8da1b, eng-b 8f18e7b,
  eng-c 5758289, eng-a 0304db3, upd 59ddaeb, rt 8d99dcb, gem 27ada08, doc f692234,
  hyg 0f2cee1, oobe fbc1a63, roster aed6bf8, notg 8affa1a, rel 59bbc72). rt2 and learn
  are absent.'
- 'Merge resolutions read with git show --remerge-diff: D1 (eng-a x eng-b, three hunks:
  the _do_restore status, the cmd_doctor blocking plus enforcement_bypass, and the
  cmd_reset union), D2 (_seed_enforcement_records_on_legacy_lock returns early with
  no enabled target), the gem comment seam, D5 (CodexRenderTarget: skills plus .codex/agents),
  and the notg doctrine seams. Integration-only commits read line by line: e126841,
  P1 1a4e210, D4 b107cfe, P2 a58250b, D6 41bb6e6, af3c8f0, notg recipe 9f893c2/8c408ab/4506a0b,
  P5 c8a3506, f4ab914.'
- Function-level AST comparison of .tess/bin/tessctl against main and all 14 branch
  heads. Only 13 definitions differ from every source version, and all are the seams
  above or clean unions (render_output_status, cmd_update, cmd_publish). No branch
  change is lost. No gate, verdict, policy, signature, self-update, guarded_write
  or symlink function differs from its reviewed branch version. core/policy/policy.yaml
  differs from main only in comments (verifier_keys and signoff_keys unchanged). The
  workflows equal rel 59bbc72. dispatch-brief equals doc. channel-guardrails equals
  notg. guardrails and verification-routing are the doc+roster+notg union. No mandatory
  verification scope and no hard-floor item is weakened.
- 'Behavioural probes: restore re-pins a hand-edited AGENTS.md and .codex/config.toml
  (RE-PINNED, snapshot). The M1 and M2 probes are recorded in the findings. The v0.2.0
  engine (not only the 0.1.4 one) rejects a wrong-key tag and a lightweight tag via
  update and self-update with SECURITY REJECT, leaves the tree byte-identical, and
  accepts the correctly signed tag (fingerprint matches pinned key). OTA kit wrongkey
  and lightweight cases: RESULT PASS. The real ~/.gnupg invariant holds.'
- 'Commands at the head: tessctl doctor OK (1100), verify OK, lock --check OK (1009).
  Targeted v02/gate/enforcement/roster/brain/verdict/signoff/self-update/ota/write-gate
  suites: 693 passed, 10 skipped, 1 xfailed. Full pytest: 2466 passed, 10 skipped,
  1 xfailed (the documented .env.example hard-floor xfail), 22 subtests passed. deid_full_diff:
  0 hits. gitleaks on 137 commits: no leaks. Base harness chat-channel guard: only
  the allowlisted CHANGELOG history, the create-tess README note and the two .env.example
  placeholder lines (decisions item 12). The demo recording is re-recorded clean.
  conductor/orchestration-budget.md is identical to the handoff and has no private
  data. It does not weaken the mandatory-verifier scope or the hard floor.'
role_verdict_raw: CLEAR
severity_counts:
  critical: 0
  high: 0
  low: 3
  medium: 2
signature:
  algorithm: gpg-detached-armor
  signature_armored: '-----BEGIN PGP SIGNATURE-----


    

    LbvjV+tx99YyuwnG3hhyg7r4

    =OdWo

    -----END PGP SIGNATURE-----

    '
  signed_at: '2026-09-24T17:16:50.280396+00:00'
  signed_content_sha256: 9e5d20fbb74a98e2ac44c9e7258486fbef33969f3aa6c37671385b31591e0cd3
summary_line: 'Reviewed the v0.2.0 release bundle PR #199 at head f4ab91447497cd4867413680d2a3e83cacb89447,
  covering every integration seam (D1-D6), the two engine fixes, P1/P2/P5 and the
  notg recipe. Found 0 CRITICAL, 0 HIGH, 2 MEDIUM, 3 LOW. The gate, verdict, policy
  and signature paths are unchanged from their reviewed versions. The eng-b enforcement
  re-pin survives the merges, and OTA wrong-key and lightweight tags are rejected
  by both engines. The mediums (Codex role files no longer self-heal; the known-issue
  remedy strips the CLAUDE.md hard floor until reset) are detected by doctor and go
  to v0.2.1, and the CHANGELOG remedy text must be corrected before tagging.'
verifier: Cyra
verifier_signoff_note: Signed with the agent-held Cyra verifier key F9321F92B4E2DF36304CB6BAA53B9C5A1F5876E8,
  which stays registered as-is for v0.2.0. No human custody ceremony took place. This
  verdict is bound to the listed git blob SHAs at head f4ab914. Any change to a covered
  path invalidates coverage and needs a fresh verdict. A CHANGELOG-only correction
  does not touch a covered path. The reviewer did not build any part of this bundle.
---

# Cyra security verdict: PR #199 (Release v0.2.0 bundle)

Signed contract: the YAML front-matter above. The signature covers only the front-matter. This body is the human-readable record.

- Head reviewed: `f4ab91447497cd4867413680d2a3e83cacb89447` (base `5c2d698`)
- Scope: what integration changed (seams D1-D6, the engine fixes e126841 and D2, P1/P2/P5, the notg recipe and demo, and orchestration-budget.md), plus the combined engine's gate, verdict, policy, enforcement, containment and OTA signature paths. The 8 protected paths the base-engine gate lists, and the byte-identical template engine, are bound by blob SHA.
- Security verdict: **CLEAR** (disposition APPROVE). Findings: 0 critical, 0 high, 2 medium, 3 low.

## Threat model
1. A prompt-injected or careless agent with workspace write access weakens runtime enforcement (hooks, sandbox, approval policy, role permissions) or doctrine, and relies on tessctl lifecycle commands to keep the change or report success.
2. A hostile or compromised upstream serves an unsigned, lightweight or wrong-key tag to `tessctl update`/`self-update`.
3. An integration merge silently drops or weakens a control that was reviewed per PR.

## Required before tagging (docs only)
Correct CHANGELOG.md:45. The issue affects every 0.1.x upgrade, and `lock --regen --only` fails there. The working remedy is: delete both files, run `tessctl update --ref v0.2.0` again, run `tessctl reset CLAUDE.md`, then confirm `tessctl doctor` is OK.

## Residual risk
- Codex per-role files (and the .claude/agents role files) keep an uncaptured hand edit, including a sandbox_mode escalation. doctor and verify fail on it. Self-healing for these files is a v0.2.1 item.
- The update ordering (Step 6 fast-forward before A2 adoption) can leave a Tess-written CLAUDE.md kept as a hand edit while update exits 0. doctor catches it as SECURITY DRIFT, and doctor does not run automatically.
- The trust root is still this agent-held verifier key. PR #181 (the P0 type-swap gate bypass fix) is deferred. Both are disclosed in the release notes.
