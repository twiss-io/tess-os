{
  "schema": 1,
  "_doc": "Tess OS Keystone manifest — hand-authored per spec kb/wiki/synthesis/2026-06-27-keystone-architecture.md §2.4. THIS IS THE UPDATER'S WRITE-SCOPE GUARD. Do NOT auto-glob: this repo mixes framework content with clients/, kb/, example-project/, .venv/ and stray root files. tessctl may write ONLY to paths matching owned_globs (refined per-file by tess.lock status). Everything else is unreachable. never_touch is an explicit belt-and-suspenders denylist for user data, secrets, and known-stray junk; precedence: owned_globs WINS over never_touch (so clients/_template/** stays framework-owned even though clients/*/** is denied).",
  "precedence": "owned_globs > never_touch",

  "render_targets": {
    "_doc": "Phase 1 (HIGH-1 / MED-3, Fable review) — PER-INSTALL render-target enablement. `tessctl render` with no flags, and `tessctl update`'s Step 7, render only the targets named in `enabled` — NOT every target registered in the RENDER_TARGETS registry (.tess/bin/tessctl). This is what stops a Claude-only install from silently starting to emit e.g. codex/AGENTS.md the moment a Phase 2+ target is added to the registry: a target must be registered AND enabled here to be rendered by default. `tessctl render --target <name>` explicitly bypasses this list (an operator naming a target by hand is not the silent-default case this guards). Missing this key entirely (a manifest that predates Phase 1) defaults to the same effective value: [\"claude-code\"].",
    "enabled": ["claude-code"]
  },

  "owned_globs": [
    "CLAUDE.md",
    "conductor/**",
    "agents/**",
    ".claude/agents/**",
    ".claude/commands/**",
    ".claude/hooks/**",
    ".claude/skills/**",
    ".claude/settings.json",
    "clients/_template/**",
    "core/contracts/**",
    "core/policy/**"
  ],

  "_owned_notes": {
    "CLAUDE.md": "entry-point doctrine; rendered from CLAUDE.md.tpl + operator/ stubs on update (status core-managed, tier security for its rule blocks).",
    "conductor/**": "the doctrine files; guardrails.md / verification-routing.md / channel-guardrails.md / dispatch-brief.md carry tier:security in tess.lock.",
    "agents/**": "165 persona prose specs (the agents/ subtree). Framework-owned per spec §2.1 CORE territory and the §2.4 manifest example. NOTE: this was NOT in the caller's parenthetical owned list; added because these are core content, not user data — omitting them would orphan every persona spec from updates. User-authored agents live at .claude/agents/<custom>.md and are protected per-file via tess.lock status:user-created, NOT by excluding this glob.",
    ".claude/agents/**": "the 42 compiled dispatch definitions. Per-file status in tess.lock governs actual write behavior: core-managed agents merge; status:user-created agents are never read, never touched.",
    ".claude/commands/**": "26 slash-command definitions (defect #9): core source at .tess/core/commands/**; live at .claude/commands/**. Workflows decision: .claude/workflows/ (3 files — daily-error-report.js, mission-execution.js, clienta-incident.js) are instance-specific operator automation, NOT framework content — they remain user-space and are excluded from core/owned_globs.",
    ".claude/hooks/**": "the 6 guard hooks. LOW-2 (Fable review): the render pipeline supports a {{TESS_ROOT}} template token (apply_token_sub in .tess/bin/tessctl), but none of the 6 shipped hooks carry it — they resolve their project root at RUN time via the $CLAUDE_PROJECT_DIR env var Claude Code injects, not at RENDER time. LF-enforced (CRLF breaks the shebang).",
    ".claude/skills/**": "framework-provided skills.",
    ".claude/settings.json": "rendered every update from settings-core.json (spec Decision 8). LOW-2 (Fable review): settings-core.json also uses $CLAUDE_PROJECT_DIR, not the {{TESS_ROOT}} token — zero core files ship the literal token today. This is WHY rendered output is byte-identical regardless of machine/root: no absolute path is ever baked into the file at render time; {{TESS_ROOT}} substitution exists in the engine (tested directly in tests/test_render.py) for a future core file that needs it, it just isn't exercised by anything currently shipped. Customize via `tessctl publish --tag settings`.",
    "clients/_template/**": "client-folder scaffold — framework-owned (spec §2.4). Distinct from real clients under clients/*/** which are never_touch.",
    "core/contracts/**": "Phase 0/1/2 contracts-as-code: brief/crew-plan/verdict/return-manifest/policy JSON schemas + README, the machine-checkable half of the doctrine (spec: docs/ULTIMATE_FRAMEWORK_PLAN.md Phase 0 Decision #3 + Phase 1 item 1, 'wire the deferred Phase-0 contracts into the managed set' + Phase 2 item 4, 'policy.schema.json... build it now'). Core source at .tess/core/contracts/**; live at core/contracts/** — same pristine-mirror pattern as conductor/**. brief.schema.json + verdict.schema.json + policy.schema.json carry tier:security in tess.lock (they encode conductor/dispatch-brief.md, conductor/verification-routing.md, and conductor/guardrails.md Rule 18, all already tier:security).",
    "core/policy/**": "Phase 2 (`tessctl gate` spine, Design Decision #2/#6): the policy-AS-DATA instance (core/policy/policy.yaml) the gate reads to classify changed paths and decide what needs a covering APPROVE verdict — the machine-readable half of conductor/verification-routing.md + conductor/guardrails.md Rule 18. tier:security in tess.lock for the same reason as the two schemas above: silently weakening this file (e.g. deleting a rule that used to gate a real prod path) is exactly as consequential as editing the doctrine prose. Core source at .tess/core/policy/**; live at core/policy/** — same pristine-mirror pattern as conductor/**."
  },

  "never_touch": [
    "kb/**",
    "clients/*/**",
    "operator/**",
    ".env",
    ".env.*",
    ".claude/settings.local.json",
    "*.local.md",
    "**/*.local.md",
    "UPGRADE-NOTES.md",
    ".tess/snapshots/**",
    ".tess/staging/**",
    ".tess/modified.json",
    ".tess/update.lock",
    ".tess/trace/**",
    "main.py",
    "pyproject.toml",
    "uv.lock",
    ".python-version",
    ".venv/**",
    "example-project/**",
    "starter/**",
    "docs/**",
    "infrastructure/**",
    ".agents/**",
    "skills-lock.json",
    ".mcp.json",
    "README.md",
    "adapters/**"
  ],

  "_never_touch_notes": {
    "kb/**": "internal + client knowledge base (raw/wiki/lint); Tess-maintained user space.",
    "clients/*/**": "real client operating systems — data, secrets, repos. clients/_template/** is the sole exception (owned_globs wins).",
    "operator/**": "user-authored identity & context, injected at CLAUDE.md render time.",
    ".env / .env.* / .claude/settings.local.json": "secrets and local allowlists; Claude Code merges settings.local.json natively.",
    "*.local.md / **/*.local.md": "append-first shadow files — the 80% customization path (spec §5 Layer 2); appended after core on resolve, never overwritten.",
    ".tess/*": "tessctl's own transactional state (snapshots, staging, provenance log, lock). NOTE: .tess/core/** is the pristine merge-base source — read by tessctl, never a live-tree write target, so it is intentionally absent from BOTH lists.",
    ".tess/trace/**": "Goal #8: the no-mission-id fallback trace-log path (.tess/trace/runs/<run_id>.jsonl) — local runtime state written directly by `_trace_append_event` (never through guarded_write / the manifest write-gate), same bucket as .tess/snapshots/** and .tess/staging/** above. Mission-scoped trace events (missions/<id>/trace.jsonl) are NOT covered by this entry — they are mission-record data, the same un-manifested bucket missions/<id>/verdicts/** already lives in.",
    "python/data layer": "main.py, pyproject.toml, uv.lock, .python-version, .venv/** — the repo's Python/uv tooling, not framework doctrine.",
    "example-project/ , starter/ , docs/ , infrastructure/ , .agents/": "non-framework subtrees present in this messy repo; explicitly fenced off.",
    "adapters/**": "Phase 1 render-target-adapter documentation seam (docs/ULTIMATE_FRAMEWORK_PLAN.md Part B architecture; item 3 of the Phase 1 brief). Pure prose describing the RenderTarget interface implemented in .tess/bin/tessctl (RENDER_TARGETS registry) — same fenced-off treatment as docs/**, since there is no core->live split to track here (no .tess/core/adapters/** mirror exists; nothing to render). Revisit if/when Phase 2+ targets ship adapter-specific config fragments that DO need core-tracking."
  }
}
