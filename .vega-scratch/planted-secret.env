# Deliberately planted fake GitHub PAT to prove the scoped gitleaks scan
# still catches a real leak introduced in a PR's own commits. Not a real
# token — randomly generated, never used anywhere, revoked-on-sight if it
# were.
GITHUB_TOKEN=ghp_K7RnoBi74Lr3DbC7ZxaEyzDGaCQOeW2mtc8o
