# Recommended GitHub description and topics

> **Recommendation only — not applied.** Repository settings are externally
> visible state. This documentation change does not mutate the live GitHub
> description, topics, visibility, releases, or branch settings.

The current live metadata remains Claude-only and no longer matches the
repository's model-neutral governance boundary. After this documentation lands,
the repository owner can review and apply the following exact values.

## Description

```text
Signed, fail-closed review gate and model-neutral governance harness for coding-agent output.
```

## Topics

Use this exact ordered set:

```text
tess-os
ai-governance
coding-agents
review-gate
policy-as-code
agent-orchestration
claude-code
openai-codex
devsecops
software-supply-chain
```

These topics describe current repository concerns without claiming universal
model support, production certification, Tess Cloud availability, or Tess Vault
availability. `claude-code` and `openai-codex` name evidenced integration
surfaces; they do not imply equal conformance levels.

## Owner follow-up

Applying these values is a separate owner-authorized GitHub settings mutation.
It should happen only after the documentation commit is reviewed and landed.
No bot, agent, documentation test, or candidate repository change should claim
that the recommendation has already been applied.
