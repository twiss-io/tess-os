"""brainlib: the Tess OS second-brain learning loop (stdlib only, Python 3.9+).

Pipeline: capture -> journal -> cue pass -> distill -> verify -> promote ->
index -> review. See docs/brain/LEARNING.md. Nothing here calls the network,
and nothing here writes a durable record without a verbatim quote that a
script can find again.
"""

VERSION = "0.2.0"
