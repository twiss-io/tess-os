# core/policy/policy.yaml — Ship-Gate Policy (Phase 2 of docs/ULTIMATE_FRAMEWORK_PLAN.md,
# Design Decision #2 "deterministic gate spine" + Design Decision #6 "verification
# produces a gateable artifact"). Validated against core/contracts/policy.schema.json
# by `tessctl gate` before it is trusted for anything (a policy file that fails its
# own schema is a fail-closed condition — see `tessctl gate`'s policy-load step).
#
# THIS FILE IS HAND-AUTHORED PER PROJECT, same posture as tess.manifest.json's own
# header ("hand-authored per spec... do NOT auto-glob"). The framework cannot guess
# which paths in YOUR repo are production code, client deliverables, or externally
# visible — that is a fact about your project, not the framework's. The shipped
# defaults below are DELIBERATELY NARROW:
#   - `tess-os-security-tier-doctrine` is the one rule that is genuinely live and
#     meaningful in THIS repo (twiss-io/tess-os) today — it protects the exact
#     files this repo's own tess.lock already tags tier:security, so silently
#     weakening the machine-checkable half of the doctrine is treated with the
#     same weight as editing the doctrine prose itself.
#   - `example-prod-service-placeholder` is a worked EXAMPLE with a glob no real
#     tree will ever match (`__REPLACE_ME__/...`) — present so `tessctl doctor`/
#     a fresh install shows the shape of a real rule, not so it silently gates
#     anything. Replace it with your project's actual prod/client-facing paths
#     on adoption; do not leave it as a false sense of coverage.
#
# M1 FIX (Fable Phase-2 adversarial review — was: "Advisory note... tessctl gate
# does not yet REJECT a covering verdict for naming a verifier outside that
# list"): `allowed_verifiers` is now ENFORCED. A covering verdict must (a)
# schema+lint validate, (b) carry disposition:APPROVE, (c) have a covers_paths
# glob matching the changed path, (d) name a `verifier` in the matched rule's
# allowed_verifiers, AND (e) record this path's CURRENT content in
# artifact_hashes (core/contracts/verdict.schema.json) — missing any of these
# means the verdict no longer counts as covering, and `tessctl gate` reports
# which condition failed.
#
# Glob semantics note (M2 fix): `**/*.env` below now ALSO matches a root-level
# `.env` (previously it required at least one directory component — see
# path_matches_globs's own header comment in .tess/bin/tessctl for the bug and
# fix); `*` inside any other segment never spans `/`.
#
# Phase 2b (verdict signing — closes the Fable Phase-2 residual: verdicts were
# committer-authored plain files with no cryptographic signature). A covering
# verdict must now ALSO carry a `signature` (core/contracts/verdict.schema.json)
# that verifies against the SAME-named entry in `verifier_keys` below — see
# docs/TRUST_BOOTSTRAP_SECURITY_DESIGN.md for the fail-closed trust boundary.
# `.tess/keys/verifiers/**` and `.tess/keys/signoffs/**` are separate,
# non-interchangeable public-key namespaces. Both are added to
# tess-os-security-tier-doctrine's globs below: tampering with either registry
# is exactly as consequential as tampering with policy.yaml itself, and
# requires its own covering Reid/Cyra verdict — closing the "who guards the
# key registry" loop. Policy lint and signature verification also reject a
# registration that redirects either role outside its dedicated namespace.
#
# MEDIUM-1 FIX (Fable Phase-2b follow-up review — the universal GitHub
# self-gating trap): `.github/workflows/**` is now ALSO in
# tess-os-security-tier-doctrine's globs. Before this fix, `.github/workflows/
# tess-gate.yml` (the gate's own CI entrypoint) was NOT covered by any
# `require_verdict` rule — once branch protection names "tessctl gate ci" as
# a required check, a malicious PR could keep that exact check name while
# neutering its step (e.g. replace the `tessctl gate ci` run with `exit 0`)
# IN THE SAME PR, and the required-check-by-name mechanism would still show
# green. Gating the workflow directory itself means any such edit is now
# `prod_touching` and needs its own covering, signed Reid/Cyra verdict —
# closing the "who guards the gate's own trigger" loop the same way
# `.tess/keys/verifiers/**` already closed "who guards the key registry."
# The authoritative workflow's hashed runtime requirements below .tess/ci/
# are governed by the same rule and read only from immutable event BASE.
# Candidate dependency pins or hashes never evaluate their own revision.
# See conductor/verdict-signing.md's "Defense-in-depth" section for the
# complementary, repo-admin-side CODEOWNERS/ruleset recommendation this does
# NOT replace (a signed verdict is a content control; CODEOWNERS/rulesets are
# an independent, GitHub-native review-gate control over the same paths).
#
# HONESTY-CAPSTONE-AUDIT-2026-07-08 FIX (§3-c + §3-d — the SAME self-
# protection class as A3/MEDIUM-1 above, applied to the two surfaces that
# were still uncovered): MEDIUM-1 gated the WORKFLOW FILE that invokes the
# gate; it never gated the ENGINE that workflow invokes (`.tess/bin/**`, plus
# the root `tessctl` wrapper that delegates to it) or the SIGN-OFF ARTIFACT
# DIRECTORY (`.tess/gate/signoffs/**`) a hard floor's clearance depends on.
# Both are now added to this rule's globs:
#   (c) same-push ENGINE tamper — `.github/workflows/tess-gate.yml` runs
#       `python3 .tess/bin/tessctl gate ci` FROM THE PUSHED CHECKOUT. An
#       attacker who edits `_gate_run_ship_check` (or any gate logic in
#       `.tess/bin/tessctl`) to return not-blocked, in the SAME push as an
#       unreviewed payload, would keep the required check green against a
#       tampered engine — no glob here caught an edit to the engine ITSELF.
#       Gating `.tess/bin/**` + `tessctl` means such an edit now needs its
#       own covering, signed Reid/Cyra verdict, same as any other doctrine
#       edit. Policy coverage alone is not sufficient against a TRULY
#       neutered engine (a tampered engine evaluates the policy that would
#       flag it) — see `.github/workflows/tess-gate.yml`'s own header for
#       the complementary CI-side fix (the gate is now invoked from a
#       PINNED, base-ref-extracted copy of the engine, never the pushed
#       tree's own copy).
#   (d) hard-floor sign-off forgery — `.tess/gate/signoffs/**` used to be
#       validated by JSON SHAPE ALONE (five non-empty fields, no signature,
#       no authentication of `authorized_by`) — ANY agent able to write a
#       file could forge a hard-floor clearance. Gating this directory means
#       introducing/editing a sign-off artifact is now ALSO `prod_touching`
#       and needs its own covering Reid/Cyra verdict, layered on top of (not
#       instead of) the new `signoff_keys`-backed cryptographic
#       authentication below — a sign-off must now be BOTH a governed
#       artifact AND signed by a registered operator key.
policy:
  version: 1
  # Stable sign-off scope identity. The ship gate trusts only this value from
  # immutable BASE policy; a candidate cannot rename itself and replay a
  # clearance issued for another repository.
  repository_id: twiss-io/tess-os

  # Immutable renderer admission inventory. Bootstrap truth: this inventory
  # is NOT authority for the PR that first introduces it because that PR's
  # BASE does not contain it. The first adoption remains owner-controlled and
  # NO-MERGE. Once committed, only this BASE copy — never candidate policy or
  # candidate lock rows — defines renderer families, canonical lock mappings,
  # and generated outputs for the next admission decision.
  renderer_admission:
    version: 1
    rule_id: tess-os-renderer-base-admission
    lock_file: .tess/tess.lock
    registry_file: .tess/bin/tessctl
    target_config_files:
      - tess.manifest.json
    family_globs:
      - .tess/core/templates/CLAUDE.md.tpl
      - .tess/core/templates/claude-md/**
      - .tess/core/settings-core.json
      - .tess/core/conductor/identity.md
      - .tess/core/conductor/personality.md
      - .tess/core/templates/client/_template/CLAUDE.md
      - .tess/core/templates/agents-md/**
      - .tess/core/commands/**
    mappings:
      - input_glob: .tess/core/templates/CLAUDE.md.tpl
        lock_live_glob: CLAUDE.md
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: template
        projections:
          - {output_glob: CLAUDE.md, mode: engine-render}
        min_matches: 1
      - input_glob: .tess/core/templates/claude-md/*.md
        lock_live_glob: CLAUDE.md
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: fragment
        projections:
          - {output_glob: CLAUDE.md, mode: engine-render}
        min_matches: 1
      - input_glob: .tess/core/settings-core.json
        lock_live_glob: .claude/settings.json
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: rendered
        projections:
          - {output_glob: .claude/settings.json, mode: engine-render}
        min_matches: 1
      - input_glob: .tess/core/conductor/identity.md
        lock_live_glob: conductor/identity.md
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: conductor/identity.md, mode: core-render}
        min_matches: 1
      - input_glob: .tess/core/conductor/personality.md
        lock_live_glob: conductor/personality.md
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: conductor/personality.md, mode: core-render}
        min_matches: 1
      - input_glob: .tess/core/templates/client/_template/CLAUDE.md
        lock_live_glob: clients/_template/CLAUDE.md
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: clients/_template/CLAUDE.md, mode: core-render}
        min_matches: 1
      - input_glob: .tess/core/templates/agents-md/AGENTS.md.tpl
        lock_live_glob: null
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: AGENTS.md, mode: engine-render}
        min_matches: 1
      - input_glob: .tess/core/templates/agents-md/*.md
        lock_live_glob: null
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: AGENTS.md, mode: engine-render}
        min_matches: 3
      - input_glob: .tess/core/templates/agents-md/codex-config.toml.tpl
        lock_live_glob: null
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: .codex/config.toml, mode: engine-render}
        min_matches: 1
      - input_glob: .tess/core/commands/*.md
        lock_live_glob: .claude/commands/*.md
        lock_name_binding: true
        canonical_status: core-managed
        canonical_tier: normal
        canonical_render: null
        projections:
          - {output_glob: .claude/commands/*.md, mode: source-copy}
          - {output_glob: .codex/prompts/*.md, mode: engine-render}
          - {output_glob: prompts/*.md, mode: engine-render}
        min_matches: 1

  rules:
    - id: tess-os-security-tier-doctrine
      description: >-
        The doctrine + contract-schema files this framework already treats as
        tier:security in .tess/tess.lock (conductor/guardrails.md,
        conductor/verification-routing.md, conductor/channel-guardrails.md,
        conductor/dispatch-brief.md) plus the schemas that make dispatch-brief
        and verification-routing machine-checkable (core/contracts/brief.schema.json,
        core/contracts/verdict.schema.json), and the gate's own policy surface
        (core/contracts/policy.schema.json, core/policy/**, the verifier
        public-key registry at .tess/keys/verifiers/** and the
        distinct human sign-off public-key registry at
        .tess/keys/signoffs/** — Phase 2b — plus the immutable-BASE CI
        dependency lock at .tess/ci/**, and the
        gate's OWN CI workflow at .github/workflows/** — MEDIUM-1, closing the
        self-gating trap where a PR could keep the required check name while
        neutering its step), PLUS — honesty-capstone-audit-2026-07-08 §3-c/
        §3-d — the gate's OWN ENGINE (.tess/bin/** + the root tessctl
        wrapper: the workflow gates its trigger FILE but was not gating the
        CODE that file invokes) and the hard-floor SIGN-OFF ARTIFACT
        DIRECTORY (.tess/gate/signoffs/**: a sign-off used to be an
        unauthenticated JSON file any agent could write). Silently weakening
        any of these is exactly as consequential as an unreviewed production
        change — treat edits here as prod_touching.
      globs:
        - conductor/guardrails.md
        - conductor/verification-routing.md
        - conductor/channel-guardrails.md
        - conductor/dispatch-brief.md
        - core/contracts/brief.schema.json
        - core/contracts/verdict.schema.json
        - core/contracts/policy.schema.json
        - core/policy/**
        - .tess/keys/verifiers/**
        - .tess/keys/signoffs/**
        - .tess/ci/**
        - .github/workflows/**
        - .tess/bin/**
        - tessctl
        - .tess/gate/signoffs/**
      classification: [prod_touching]
      require_verdict: true
      allowed_verifiers: [Reid, Cyra]

    - id: tess-os-renderer-base-admission
      description: >-
        Immutable-BASE renderer admission surface. The committed BASE policy
        inventory, BASE lock rows, BASE engine registry, and BASE source blobs
        define required renderer ownership. Candidate policy/lock bytes may
        prove a coherent upgrade but cannot delete, remap, downgrade, or
        re-hash that authority to approve their own generated doctrine.
      globs:
        - .tess/tess.lock
        - .tess/bin/tessctl
        - tess.manifest.json
        - .tess/core/templates/CLAUDE.md.tpl
        - .tess/core/templates/claude-md/**
        - .tess/core/settings-core.json
        - .tess/core/conductor/identity.md
        - .tess/core/conductor/personality.md
        - .tess/core/templates/client/_template/CLAUDE.md
        - .tess/core/templates/agents-md/**
        - .tess/core/commands/**
        - CLAUDE.md
        - .claude/settings.json
        - conductor/identity.md
        - conductor/personality.md
        - clients/_template/CLAUDE.md
        - AGENTS.md
        - .codex/config.toml
        - .claude/commands/*.md
        - .codex/prompts/*.md
        - prompts/*.md
      classification: [prod_touching]
      require_verdict: true
      allowed_verifiers: [Reid, Cyra]

    - id: example-prod-service-placeholder
      description: >-
        EXAMPLE / PLACEHOLDER — replace with globs matching this project's
        real production service code and client-facing surfaces once adopted
        into an actual product repo (e.g. "src/api/**", "infra/prod/**",
        "app/services/payment/**"). Ships pointed at a glob no real tree will
        ever match, so a fresh install does not silently gate on a rule
        nobody actually wrote — mirrors tess.manifest.json's own
        "hand-authored per spec, not auto-globbed" stance.
      globs:
        - "__REPLACE_ME__/prod/**"
      classification: [prod_touching, client_facing]
      require_verdict: true
      allowed_verifiers: [Reid, Quinn]

  # verifier_keys — Phase 2b's ALLOWED-KEY SET (core/contracts/policy.schema.json
  # $defs.VerifierKeyEntry). DELIBERATELY EMPTY in this shipped default, same
  # "hand-authored per project, do NOT fabricate" posture as every other
  # placeholder in this file — honest disclosure, not an oversight:
  #
  #   Until a real entry exists here for a given verifier name, NO signature
  #   can ever verify for that verifier (there is no key to check it against),
  #   so NONE of that verifier's verdicts can ever cover a path — fail-closed,
  #   not fail-open. Concretely: `tess-os-security-tier-doctrine` above already
  #   names Reid + Cyra as its allowed_verifiers; until this project's real
  #   maintainers generate and register Reid's and Cyra's actual signing keys
  #   below, that rule is UNSATISFIABLE by any verdict — every future change to
  #   conductor/guardrails.md, core/policy/**, etc. requires the hard-floor-
  #   style manual escalation this file's own header already documents for an
  #   unmet rule, not a silent bypass. This is a deliberate, disclosed
  #   consequence of shipping verdict signing without also fabricating a
  #   throwaway "Reid" identity that could be mistaken for a real trust anchor.
  #
  # Trust-anchor registration is deliberately NOT a self-service policy edit,
  # key-generation command, lockfile re-pin, or candidate-pull-request step.
  # An unreviewed candidate cannot establish the authority that clears its
  # own gate. The first anchor, recovery, rotation, and any user-facing Trust
  # Center are Xavier-owned, user-present custody decisions; until that design
  # exists, `TRUST_BOOTSTRAP_REQUIRED` is the correct outcome. See
  # docs/TRUST_BOOTSTRAP_SECURITY_DESIGN.md.
  verifier_keys: {}

  # signoff_keys — honesty-capstone-audit-2026-07-08 §3-d FIX's ALLOWED-KEY
  # SET for hard-floor SIGN-OFF artifacts (core/contracts/policy.schema.json
  # $defs.VerifierKeyEntry — the SAME entry shape as verifier_keys above).
  # DELIBERATELY EMPTY in this shipped default, same "hand-authored per
  # project, do NOT fabricate" posture as verifier_keys:
  #
  #   Before this fix, a `.tess/gate/signoffs/<rule-id>.signoff.json` artifact
  #   was accepted on JSON SHAPE ALONE (five non-empty fields: rule_id,
  #   category, authorized_by, rationale, authorized_at) — no signature, no
  #   authentication of `authorized_by` at all. Any agent able to write a
  #   file could forge a hard-floor clearance for credentials/money-movement/
  #   destructive-prod-data changes — mechanically FALSE against docs (incl.
  #   AGENTS.md, see the audit's R11) claiming "the gate treats [a
  #   .tess/gate/signoffs/ edit] as tamper and fails closed": writing a
  #   sign-off CLEARED the floor, it did not trip anything.
  #
  #   Schema v2 also closes cross-revision replay. A valid sign-off binds
  #   this repository_id from immutable BASE, the exact event BASE, the
  #   exact reviewed payload parent, the effective rule/category, and every
  #   matched path's immutable Git blob id. The attestation HEAD must be a
  #   single-parent commit changing exactly the complete required sign-off
  #   path set and nothing else; authoritative CI separately requires an exact
  #   two-parent evaluation merge whose second parent and tree are that
  #   attestation HEAD. V1/unversioned artifacts, merge/multi-head
  #   events, later commits, extra paths, symlinks/type swaps, expired
  #   attestations, and TTLs over 24 hours all fail closed before GPG.
  #   After every BASE-derived required sign-off validates, only those exact
  #   canonical files are removed from ordinary verdict matching. This
  #   narrowly breaks the payload-SHA/signoff-blob/verdict-hash cycle; it is
  #   atomic and never extends to a glob, directory, candidate-added rule,
  #   candidate key, or extra signoff-looking path.
  #
  #   Until a real entry exists here for a given `authorized_by` name, NO
  #   signature can ever verify for that name (there is no key to check it
  #   against), so a sign-off claiming that name can NEVER clear a hard floor
  #   — fail-closed, not fail-open. Concretely: with signoff_keys shipped
  #   empty, EVERY hard-floor category below is UNSATISFIABLE by any sign-off
  #   until this project's real operator generates and registers their own
  #   signing key — the same deliberate, disclosed consequence
  #   verifier_keys: {} already documents above, applied to the sign-off
  #   escape valve.
  #
  # Sign-off authority has the same boundary: it is not created through a
  # candidate change, manual policy edit, key-generation command, or lockfile
  # re-pin. During a ship-gate run, both an established registration and its
  # public-key bytes come from the immutable base revision, never candidate
  # checkout bytes. A future user-present custody design must define
  # registration, recovery, and rotation before a sign-off key can become
  # effective. Until then, a hard floor remains intentionally unsatisfiable
  # rather than silently accepting a self-issued clearance.
  signoff_keys: {}

  hard_floor_rules:
    - id: credentials
      category: credentials
      description: >-
        guardrails.md Rule 18 hard floor #1 — "Credentials — use beyond
        existing scope, change, or rotation." Not reliably path-detectable in
        general; illustrative globs only. Extend with this project's real
        secret/config surfaces on adoption. Per Rule 18 and
        ULTIMATE_FRAMEWORK_PLAN.md's gate-spine spec, a match here is NEVER
        satisfiable by a verifier's verdict alone — `tessctl gate` requires an
        explicit human sign-off artifact at
        .tess/gate/signoffs/<id>.signoff.json regardless of any verdict.
        The artifact must use schema v2, bind the exact immutable payload
        parent and full matched path/blob set, remain within its short
        validity window, and carry a signature verified against immutable
        BASE signoff_keys bytes. Unsigned, v1, stale, replayed, expired, or
        candidate-key-authenticated sign-offs cannot clear this floor.
      globs:
        - "**/*.env"
        - "**/*.env.*"
        - "**/vault.age"
        - "**/*identity.age"
        - "**/secrets/**"

    - id: money-movement
      category: money_movement
      description: >-
        guardrails.md Rule 18 hard floor #2 — "Money movement — refunds,
        voids, transfers, any payment operation." PLACEHOLDER globs — name
        this project's real payment/billing code paths on adoption.
      globs:
        - "__REPLACE_ME__/payments/**"

    - id: destructive-prod-data
      category: destructive_prod_data
      description: >-
        guardrails.md Rule 18 hard floor #3 — "Destructive production data
        operations — deletes, truncates, irreversible migrations."
        PLACEHOLDER globs — name this project's real migration/ops-script
        paths on adoption.
      globs:
        - "__REPLACE_ME__/migrations/**destructive**"

    - id: client-external-claims
      category: client_external_claims
      description: >-
        guardrails.md Rule 18 hard floor #4 — "Client-external communications
        containing new factual claims." NOT path-detectable at all — this is
        a communication-CONTENT class, not a file-path class. Ships with an
        empty globs list ON PURPOSE: `tessctl gate` mechanically catches the
        other three hard-floor categories when their globs match a changed
        path, but this one remains a pure human-process control the gate
        cannot see into from a git diff. Stated here explicitly rather than
        silently omitted (the same "explicit non-coverage over silent gap"
        discipline core/contracts/README.md already applies to Phase 0's
        deferred roster/guild-membership checks).
      globs: []
