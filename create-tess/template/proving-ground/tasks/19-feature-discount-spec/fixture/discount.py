def apply_discount(price_cents, discount_percent):
    """Reduce `price_cents` by `discount_percent` percent and return the
    result as a whole number of cents, floored. Raise ValueError if the
    percent is outside [0, 100].

    See brief.md for all four rules. Implement this — do not leave the stub
    in place.
    """
    raise NotImplementedError
